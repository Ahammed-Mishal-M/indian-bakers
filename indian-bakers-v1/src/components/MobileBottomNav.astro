---
---

<nav class="md:hidden fixed bottom-4 left-4 right-4 z-[60]">
  <div class="bg-[#111111]/90 backdrop-blur-lg border border-white/10 rounded-2xl shadow-2xl px-2 py-3 grid grid-cols-5 items-center">
    
    <!-- Home -->
    <a href="#home" class="nav-item group flex flex-col items-center gap-1 text-white/50 hover:text-[#E9A127] transition-colors" data-target="home">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-[.active]:stroke-[#E9A127]"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
      <span class="text-[10px] font-medium tracking-wide group-[.active]:text-[#E9A127]">Home</span>
    </a>

    <!-- Menu -->
    <a href="#menu" class="nav-item group flex flex-col items-center gap-1 text-white/50 hover:text-[#E9A127] transition-colors" data-target="menu">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-[.active]:stroke-[#E9A127]"><line x1="8" y1="6" x2="21" y2="6"></line><line x1="8" y1="12" x2="21" y2="12"></line><line x1="8" y1="18" x2="21" y2="18"></line><line x1="3" y1="6" x2="3.01" y2="6"></line><line x1="3" y1="12" x2="3.01" y2="12"></line><line x1="3" y1="18" x2="3.01" y2="18"></line></svg>
      <span class="text-[10px] font-medium tracking-wide group-[.active]:text-[#E9A127]">Menu</span>
    </a>

    <!-- Specials -->
    <a href="#specials" class="nav-item group flex flex-col items-center gap-1 text-white/50 hover:text-[#E9A127] transition-colors" data-target="specials">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-[.active]:stroke-[#E9A127]"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275Z"/></svg>
      <span class="text-[10px] font-medium tracking-wide group-[.active]:text-[#E9A127]">Specials</span>
    </a>

    <!-- Story (Experience) -->
    <a href="#experience" class="nav-item group flex flex-col items-center gap-1 text-white/50 hover:text-[#E9A127] transition-colors" data-target="experience">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-[.active]:stroke-[#E9A127]"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
      <span class="text-[10px] font-medium tracking-wide group-[.active]:text-[#E9A127]">Story</span>
    </a>

    <!-- Locate -->
    <a href="#locate-us" class="nav-item group flex flex-col items-center gap-1 text-white/50 hover:text-[#E9A127] transition-colors" data-target="locate-us">
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-[.active]:stroke-[#E9A127]"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>
      <span class="text-[10px] font-medium tracking-wide group-[.active]:text-[#E9A127]">Locate</span>
    </a>
    
  </div>
</nav>

<script>
  // Simple active state handling
  const navItems = document.querySelectorAll('.nav-item');
  const sections = document.querySelectorAll('section, footer');

  // Click handler
  navItems.forEach(item => {
    item.addEventListener('click', () => {
      navItems.forEach(nav => nav.classList.remove('active'));
      item.classList.add('active');
    });
  });

  // Scroll spy
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        let id = entry.target.getAttribute('id');
        if (!id) return;
        
        // Map footer contact to locate-us if needed, or keeping explicit mapping
        // Currently 'locate-us' component uses id 'locate-us' usually.
        // Let's verify LocateUs id.
        
        navItems.forEach(item => {
           // Direct href matching
           const href = item.getAttribute('href')?.substring(1);
           if (href === id) {
             navItems.forEach(nav => nav.classList.remove('active'));
             item.classList.add('active');
           }
        });
      }
    });
  }, { threshold: 0.3 }); 

  sections.forEach(section => observer.observe(section));
</script>
